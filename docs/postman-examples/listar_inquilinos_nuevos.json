{
  "name": "Listar Inquilinos Nuevos (Status=1)",
  "request": {
    "method": "GET",
    "header": [],
    "url": {
      "raw": "{{base_url}}/api/v1/inquilinos?status=1",
      "host": [
        "{{base_url}}"
      ],
      "path": [
        "api",
        "v1",
        "inquilinos"
      ],
      "query": [
        {
          "key": "status",
          "value": "1",
          "description": "Filtrar por status Nuevo"
        }
      ]
    },
    "description": "Lista solo los inquilinos con status = 1 (Nuevo). Estos son prospectos recién registrados que requieren atención."
  },
  "response": [],
  "auth": {
    "type": "bearer",
    "bearer": [
      {
        "key": "token",
        "value": "{{access}}",
        "type": "string"
      }
    ]
  }
}
